// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by lcm-gen

#include <string.h>
#include "vehicle_sim_lidar_result_t.h"

static int __vehicle_sim_lidar_result_t_hash_computed;
static uint64_t __vehicle_sim_lidar_result_t_hash;

uint64_t __vehicle_sim_lidar_result_t_hash_recursive(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __vehicle_sim_lidar_result_t_get_hash)
            return 0;

    __lcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__vehicle_sim_lidar_result_t_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x8bf358d38c8be938LL
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __vehicle_sim_lidar_result_t_get_hash(void)
{
    if (!__vehicle_sim_lidar_result_t_hash_computed) {
        __vehicle_sim_lidar_result_t_hash = (int64_t)__vehicle_sim_lidar_result_t_hash_recursive(NULL);
        __vehicle_sim_lidar_result_t_hash_computed = 1;
    }

    return __vehicle_sim_lidar_result_t_hash;
}

int __vehicle_sim_lidar_result_t_encode_array(void *buf, int offset, int maxlen, const vehicle_sim_lidar_result_t *p, int elements)
{
    int pos = 0, element;
    int thislen;

    for (element = 0; element < elements; element++) {

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].distance), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].intensity), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].lidar_angle), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int vehicle_sim_lidar_result_t_encode(void *buf, int offset, int maxlen, const vehicle_sim_lidar_result_t *p)
{
    int pos = 0, thislen;
    int64_t hash = __vehicle_sim_lidar_result_t_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __vehicle_sim_lidar_result_t_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int __vehicle_sim_lidar_result_t_encoded_array_size(const vehicle_sim_lidar_result_t *p, int elements)
{
    int size = 0, element;
    for (element = 0; element < elements; element++) {

        size += __double_encoded_array_size(&(p[element].distance), 1);

        size += __double_encoded_array_size(&(p[element].intensity), 1);

        size += __double_encoded_array_size(&(p[element].lidar_angle), 1);

    }
    return size;
}

int vehicle_sim_lidar_result_t_encoded_size(const vehicle_sim_lidar_result_t *p)
{
    return 8 + __vehicle_sim_lidar_result_t_encoded_array_size(p, 1);
}

int __vehicle_sim_lidar_result_t_decode_array(const void *buf, int offset, int maxlen, vehicle_sim_lidar_result_t *p, int elements)
{
    int pos = 0, thislen, element;

    for (element = 0; element < elements; element++) {

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].distance), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].intensity), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].lidar_angle), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __vehicle_sim_lidar_result_t_decode_array_cleanup(vehicle_sim_lidar_result_t *p, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __double_decode_array_cleanup(&(p[element].distance), 1);

        __double_decode_array_cleanup(&(p[element].intensity), 1);

        __double_decode_array_cleanup(&(p[element].lidar_angle), 1);

    }
    return 0;
}

int vehicle_sim_lidar_result_t_decode(const void *buf, int offset, int maxlen, vehicle_sim_lidar_result_t *p)
{
    int pos = 0, thislen;
    int64_t hash = __vehicle_sim_lidar_result_t_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __vehicle_sim_lidar_result_t_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int vehicle_sim_lidar_result_t_decode_cleanup(vehicle_sim_lidar_result_t *p)
{
    return __vehicle_sim_lidar_result_t_decode_array_cleanup(p, 1);
}

int __vehicle_sim_lidar_result_t_clone_array(const vehicle_sim_lidar_result_t *p, vehicle_sim_lidar_result_t *q, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __double_clone_array(&(p[element].distance), &(q[element].distance), 1);

        __double_clone_array(&(p[element].intensity), &(q[element].intensity), 1);

        __double_clone_array(&(p[element].lidar_angle), &(q[element].lidar_angle), 1);

    }
    return 0;
}

vehicle_sim_lidar_result_t *vehicle_sim_lidar_result_t_copy(const vehicle_sim_lidar_result_t *p)
{
    vehicle_sim_lidar_result_t *q = (vehicle_sim_lidar_result_t*) malloc(sizeof(vehicle_sim_lidar_result_t));
    __vehicle_sim_lidar_result_t_clone_array(p, q, 1);
    return q;
}

void vehicle_sim_lidar_result_t_destroy(vehicle_sim_lidar_result_t *p)
{
    __vehicle_sim_lidar_result_t_decode_array_cleanup(p, 1);
    free(p);
}

int vehicle_sim_lidar_result_t_publish(lcm_t *lc, const char *channel, const vehicle_sim_lidar_result_t *p)
{
      int max_data_size = vehicle_sim_lidar_result_t_encoded_size (p);
      uint8_t *buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = vehicle_sim_lidar_result_t_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = lcm_publish (lc, channel, buf, data_size);
      free (buf);
      return status;
}

struct _vehicle_sim_lidar_result_t_subscription_t {
    vehicle_sim_lidar_result_t_handler_t user_handler;
    void *userdata;
    lcm_subscription_t *lc_h;
};
static
void vehicle_sim_lidar_result_t_handler_stub (const lcm_recv_buf_t *rbuf,
                            const char *channel, void *userdata)
{
    int status;
    vehicle_sim_lidar_result_t p;
    memset(&p, 0, sizeof(vehicle_sim_lidar_result_t));
    status = vehicle_sim_lidar_result_t_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding vehicle_sim_lidar_result_t!!!\n", status);
        return;
    }

    vehicle_sim_lidar_result_t_subscription_t *h = (vehicle_sim_lidar_result_t_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    vehicle_sim_lidar_result_t_decode_cleanup (&p);
}

vehicle_sim_lidar_result_t_subscription_t* vehicle_sim_lidar_result_t_subscribe (lcm_t *lcm,
                    const char *channel,
                    vehicle_sim_lidar_result_t_handler_t f, void *userdata)
{
    vehicle_sim_lidar_result_t_subscription_t *n = (vehicle_sim_lidar_result_t_subscription_t*)
                       malloc(sizeof(vehicle_sim_lidar_result_t_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->lc_h = lcm_subscribe (lcm, channel,
                                 vehicle_sim_lidar_result_t_handler_stub, n);
    if (n->lc_h == NULL) {
        fprintf (stderr,"couldn't reg vehicle_sim_lidar_result_t LCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int vehicle_sim_lidar_result_t_subscription_set_queue_capacity (vehicle_sim_lidar_result_t_subscription_t* subs,
                              int num_messages)
{
    return lcm_subscription_set_queue_capacity (subs->lc_h, num_messages);
}

int vehicle_sim_lidar_result_t_unsubscribe(lcm_t *lcm, vehicle_sim_lidar_result_t_subscription_t* hid)
{
    int status = lcm_unsubscribe (lcm, hid->lc_h);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe vehicle_sim_lidar_result_t_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

